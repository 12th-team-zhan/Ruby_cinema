<!-- 連結購物車後刪除 -->
訂票紀錄 <%= link_to "新增訂單" , new_order_path %>
<!-- 連結購物車後刪除 -->

<div class="container text-center">
  <div class="row d-flex justify-content-evenly">
    <div class="col-12 col-md-7 col-lg-8">
      <div class="container p-3">
        <div class="col text-start">
          <h2>訂票信箱 <%= @user.email %></h2>
          <p class="fw-lighter">
            若購票失敗您的付款授權稍後將被取消不會收取任何費用，可於 30
            分後重新確認。
          </p>
        </div>
        <div class="col">
          <div class="container text-center">
            <div class="row">
              <table
                class="table table-striped"
                data-controller="order-content"
              >
                <thead>
                  <tr class="table-secondary">
                    <th scope="col" class="col-3">訂購時間</th>
                    <th scope="col" class="col-7">訂購內容</th>
                    <th scope="col" class="col-2"></th>
                  </tr>
                </thead>
                <tbody>
                  <% @orders.each do |order| %>
                  <tr>
                    <td>
                      <p><%= order.created_at.strftime("%Y/%m/%d %I:%M") %></p>
                      <p><%= order.status %></p>
                      <div data-id="<%= order.id %>" data-canceled="123">
                        <%= link_to "取消訂單", cancel_order_path(order),
                        method:"patch", class:"btn btn-secondary btn-md mx-2
                        check_order_btn",
                        data:{action:"click->order-content#cancel"} %>
                      </div>
                    </td>
                    <td class="text-start mx-3">
                      <p>取票序號: <%= order.serial %></p>
                      <p>電影名稱：</p>
                      <p>影城：</p>
                      <p>場次： 2022-11-20 16:25</p>
                      <div class="d-flex justify-content-between">
                        <p>總計金額: <%= order.amount %></p>
                      </div>
                    </td>
                    <td>
                      <button
                        class="btn btn-secondary btn-md"
                        data-action="click->order-content#read"
                      >
                        查看訂單
                      </button>
                    </td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="col-12 col-md-5 col-lg-4"><%= render "member_card" %></div>
  </div>
</div>
